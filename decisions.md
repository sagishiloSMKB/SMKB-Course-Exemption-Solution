# יומן החלטות — פטור מקורסים

**סמינר הקיבוצים (SMKB)** · פתרון `SMKBCourseExemption` · קידומת `cex`

---

## מהו הקובץ הזה

זהו תיעוד ה**"למה"** של הפרויקט. `PRODUCT_SPEC.md` מתאר **מה** המוצר צריך לעשות;
`SOLUTION-SPEC.md` יתאר **איך** הוא ייבנה; הקובץ הזה מסביר **מדוע** נבחרה כל דרך, ומה המחיר שלה.

הקובץ נכתב לפני `SOLUTION-SPEC.md` בכוונה — כך שאם עבודת המפרט הטכני משתבשת, ההחלטות עצמן שרדו.
הוא גם שורד את הפעלת Claude Code מחדש הנדרשת ב-Init Project שלב 6.3, שבה כל מה שלא נכתב לקובץ נעלם.

**מוסכמות כתיבה:** טקסט בעברית; כל מזהה טכני מופיע ב-`backticks` כדי שכיווניות הטקסט תישאר יציבה;
תוכן מובנה מופיע בטבלאות, שבהן העברית והמונחים הלועזיים יושבים בתאים נפרדים.

**מקורות:** `PRODUCT_SPEC.md` (2,109 שורות), `SOLUTION-SPEC.md` (תבנית),
`SECURITY-BASELINE.md`, `CLAUDE.md`, ושלושה סקרים מלאים של ה-starters.

---

## פעולות המשך — מחוץ לקוד

משימות תפעוליות שאינן ניתנות לביצוע בתוך המאגר. חלקן חוסמות שלבים מאוחרים יותר.

| # | פעולה | נדרש עבור | סטטוס |
|---|---|---|---|
| 1 | **בקשה מרוכזת ל-IT** — חריגת Group Policy ל-PowerShell, גישה ל-`Azure Key Vault`, ופתיחת תיבת דואר משותפת. **לשלוח כבקשה אחת** | `D-03`, `D-07`, `D-10`, וכל `deploy.ps1` בשלב 8 | פתוח |
| 2 | **הרשאת יצירת מאגרים בארגון `SMKB-AC-IL`** בגיטהאב | העברת המאגר מהחשבון האישי `sagishiloSMKB` לארגון — ההעברה חסומה עד לקבלת ההרשאה | ממתין לאישור |
| 3 | **בחירת שם לתיבת הדואר המשותפת** — `course-exemption@` / `pturim@` או אחר | `D-07`, וכל עשר ההודעות `M-01`–`M-10` | פתוח |
| 4 | **הורדת התבנית הרשמית** (`Word`) של אישור הפטור מ-SharePoint של המכללה | `D-03` — בינתיים נבנה מול תבנית זמנית | פתוח |
| 5 | **בדיקה משפטית** של הודעת הפרטיות ותקופות השמירה | `D-11`, `D-13` | פתוח |
| 6 | **סקירת רכז/ת הנגישות** לפני קידום ל-Stage | `D-12` | פתוח |

> **פעולה 1 היא הדחופה ביותר.** PowerShell חסום במכונה הזו על ידי Group Policy, ולכן
> `apply-config.ps1` (שלב 6.2) וכל סקריפטי הפריסה (שלב 8) אינם ניתנים להרצה כרגע. שלוש הבקשות
> נוגעות לאותו צוות ולכן עדיף לאגד אותן.

---

# ההחלטות

---

## `D-01` — ביטול ההערכה המקדימה: כולם "מועמדים"

**תאריך:** 2026-08-26

### מה השתנה

בוטלה כליל ההבחנה בין פונה טרום-הרשמה לבין סטודנט/ית רשומ/ה. **כל מי שמגיש/ה הוא/היא "מועמד/ת
לתוכנית X"**, ללא קשר למעמד במכללה. המערכת מפיקה אישור פטור; מה שהמדור לענייני סטודנטים עושה
עם האישור נמצא מחוץ לתחום הפתרון.

### נימוק

ההבחנה נוצרה כדי לפתור מתח אמיתי: אם פונה שטרם נרשם/ה יכול/ה לקבל פטור מחייב, המכללה מתחייבת
כלפי מי שאין לה איתו/ה שום קשר. הפתרון שנבחר במפרט המקורי היה "הערכה מקדימה" שהופכת אוטומטית
לבקשה רשמית עם ההרשמה.

**המחיר של הפתרון הזה התברר כגבוה מהתועלת.** הוא הצריך מנגנון שאיש אינו יודע איך לבנות: משהו
שיזהה שאדם נרשם ללימודים. אין במכללה מערכת שאפשר לשאול אותה, ולכן כל המסלול `J-PRO-01` נשען על
אינטגרציה שאינה קיימת. במקום להמציא מנגנון זיהוי הרשמה, בוטלה ההבחנה עצמה.

### מה יוצא מהמפרט

| מזהה | סוג | הערה |
|---|---|---|
| `R-07` | כלל | הערכות לפני הרשמה |
| `M-05` | הודעה | "ההערכה המקדימה מוכנה" |
| `M-09` | הודעה | "ההערכה הפכה לבקשה רשמית" |
| `S-P-12` | מסך | תצוגת ההערכה המקדימה |
| `J-PRO-01` | מסלול | מסע הפונה טרום-הרשמה |
| `P-2` | פרסונה | הפונה המתעניין (אמיר) |

### מקומות נוספים שנושאים שאריות של המנגנון

אלה לא הופיעו ברשימה המקורית ואותרו בסריקה. אם לא יטופלו, יישארו במוצר סמנטיקות שאיש לא יוכל להסביר.

| מקום | מה משתנה |
|---|---|
| `§8.1` — כוונת הצבע | התפקיד החמישי, **"זמני" (Provisional)**, נועד אך ורק להערכות. הוא הופך למיותר ויש להסירו |
| `S-P-03` — לוח הבקשות | המצב הרביעי, "מעורב, כולל הערכות", מתבטל. **ארבעה מצבים הופכים לשלושה** |
| `S-R-01` — תור הבדיקות | הסרת העמודה "האם הומר מהערכה" |
| `S-R-02` — סביבת הבדיקה | פריט ההקשר החמישי מאבד את "ההערכה והיסטוריית ההערכה, מסומנות כלא-מחייבות" |
| `S-P-04`, `S-P-08` | שניהם נושאים מסגור טרום-הרשמה, כולל המחרוזת `עדיין לא נרשמת ללימודים…` — להסיר מהמסכים **וגם** מנספח ב' |
| `§2.1` — אוצר מילים | המונח `הערכה מקדימה` הופך לבלתי-רלוונטי |

בנוסף: שורות נספח א' עבור `S-P-12`, `J-PRO-01`, `M-05`, `M-09`, `P-2`.

### השלכות טכניות

**פישוט משמעותי במודל הנתונים.** התכנון הקודם דרש טבלת בקשות אחת עם עמודת `Kind`
(הערכה / רשמית), כדי שהראיות "יעברו" בהמרה בלי שהסטודנט/ית יזין/תזין אותן מחדש.
**העמודה הזו מיותרת עכשיו.** טבלת בקשות אחת, מחזור חיים אחד, ללא מצב ביניים.

שתי הסתייגויות שהועלו קודם לכן בבדיקה הטכנית מתבטלות מאליהן:
- **סתירה פנימית ב-`M-05`** — המפרט קבע שההערכה מגיעה "באותו לוח זמנים כמו בקשה רשמית"
  (עשרה ימי עבודה), אך גם רמז שההערכה אינה עוברת דרך תור הבודקים. הסתירה נעלמת עם המנגנון.
- **זיהוי הרשמה** — היה הפריט החוסם היחיד בניתוח; אינו רלוונטי עוד.

### מספור פרסונות

**נשאר פער ב-`P-2`. אין מספור מחדש.** זו אותה גישה שנוקט `INIT_PROJECT.md` עצמו עם `H3` ו-`H6`
החסרים: מספור מחדש כדי "לסגור" פער מפנה בשקט כל הפניה קיימת למספר אחר.

---

## `D-02` — ראיות נשמרות ב-File Columns של Dataverse

**תאריך:** 2026-08-26

### מה השתנה

קבצי הראיות שהסטודנט/ית מעלה (גיליונות ציונים, תעודות) יישמרו ב-**`File Column` של Dataverse**,
ולא ב-SharePoint.

### נימוק

שתי סיבות, והשנייה היא המכרעת:

1. **`Critical Rule 6` מחייב זאת.** `CLAUDE.md` קובע ש-Dataverse היא פלטפורמת הנתונים, ו-SharePoint
   הוא "נתיב תאימות מדור קודם, לא בחירת אחסון".
2. **גיליון ציונים הוא הפריט הרגיש ביותר שהמערכת מחזיקה.** מודל הבעלות ברמת השורה של Dataverse
   הוא אמצעי הבקרה היחיד שיש לנו — הרשאות טבלה **אינן נאכפות בתוך Cloud Flow**, ולכן כל ההגנה
   נשענת על ה-scaffold של `§15` שמזהה את המשתמש מתוך שורת ה-session. אחסון ב-SharePoint מרחיב את
   משטח הגישה אל מעבר למודל הזה.

### השלכות — כולן שליליות, וכולן ידועות מראש

| השלכה | פירוט |
|---|---|
| אין דוגמת `File Column` ב-starter | ה-Tables starter מדגים **רק** `nvarchar` כעמודה מותאמת חיה. `<Type>file</Type>` אינו מופיע באף קובץ XML |
| אין פעולת כתיבה ב-flow | אין `UploadFile`, אין `documentbody`, אין דפוס `annotation` בשום מקום בערכה |
| **אין תקדים כלשהו להורדה** | סריקה של שני ה-starters ושל ספריית הרכיבים אחר `Blob(`, `createObjectURL`, `annotation`, "Get file content" — **אפס תוצאות** |
| הדפוס היחיד בערכה הוא SharePoint | `CreateAttachment` עם base64 — כלומר בחרנו את הדרך שאין לה תבנית |

**המסקנה המעשית:** גם עמודת ה-XML וגם פעולת ה-flow ייכתבו בפעם הראשונה כאן.
**מומלץ לבצע spike מוקדם** — טבלה אחת עם `File Column` אחת, מחזור כתיבה-קריאה מלא, ופריסה —
לפני שמודל הנתונים כולו נבנה סביב ההנחה הזו.

**העלאה עצמה נתמכת היטב:** `buildNamedFilePayload` בצד הלקוח → base64 → flow →
ולידציה בצד השרת לפי `§16` (רשימת סיומות + magic bytes + תקרת גודל + שם קובץ שהשרת מייצר).

### ⚠️ מוסכמה שיש לשמור: הצהרת SharePoint ב-`§7` היא **שורת טבלה**, לא ציטוט

הכלל `specDeclaresSharePoint` (`rules.mjs:552-561`) מדלג על כל שורה שמתחילה ב-`>`. התיעוד שלו קובע
את המוסכמה במפורש: *"הנחיות חיות בציטוטים, הצהרה אמיתית היא שורת טבלה"*.

**המלכודת:** אם בעתיד תידרש אינטגרציה אמיתית עם SharePoint והיא תוצהר **בתוך ציטוט**, הכלל יחזיר
`false`, ואזהרת `sharepoint-data-action` **תיוותר דולקת** — כלומר ההצהרה לא תעבוד. ובכיוון ההפוך,
כשההצהרה כן נתפסת, הכלל **משתיק את האזהרה לכל הפתרון**
(`if (ctx?.sharePointDeclared) return out`), כך ששימוש לא-מוצהר ב-SharePoint לא ייראה יותר בסקירה.

**ההקשר:** משפט השלילה של `D-02` ב-`§7` היה כתוב כפרוזה, והכלל — שהוא התאמת מחרוזת פשוטה ללא זיהוי
שלילה ובלי הבנה של עברית — קרא את **"אין SharePoint"** כהצהרה על SharePoint. התוצאה הייתה
שהמשפט שמצהיר על `D-02` **השתיק בשקט את השומר שאוכף את `D-02`**. התיקון: המשפט הועבר לציטוט, לפי
המוסכמה של הכלל עצמו. אין מנגנון פטור אחר — אין הערה, אין `disable`, ואין רשימת היתר.

---

## `D-03` — אישור הפטור מופק ב-Word Online וממיר ל-PDF

**תאריך:** 2026-08-26

### מה השתנה

אישור הפטור יופק מתבנית `Word`, יומר ל-`PDF`, וייחסן ב-`File Column` של Dataverse (עקבי עם `D-02`).
לשם כך תתווסף **התייחסות חיבור חמישית (`connection reference`) — `Word Online (Business)`**.

### נימוק

`§12.2` של המפרט קובע שהאישור הוא **"נקודת הסיום היחידה"** של התהליך: אין מערכת במורד הזרם
שמקבלת את ההחלטה, ומה שהסטודנט/ית יכול/ה להוכיח — הוא מה שמוחזק בידיו/ה. זהו משטח מוצר, לא
פורמט ייצוא. Word → PDF היא הדרך המעשית להפיק מסמך עברי, מודפס, שנשמר נכון בהדפסה בשחור-לבן.

### ⏰ תלות נסתרת — מועד אחרון: שלב `8.1a`

**התייחסות חיבור אינה ניתנת להסרה על ידי ייבוא חוזר.** ייבוא של פתרון unmanaged הוא `upsert`,
ול-`pac solution` יש `add-solution-component` **ללא פעולת הסרה מקבילה**. המשמעות: התייחסות החיבור
ל-Word Online תלווה את הפתרון הזה לצמיתות.

לכן ההחלטה חייבת להתקבל ב-**Init Project שלב `8.1a`, לפני הפריסה הראשונה** — יחד עם ההחלטה האם
לגזום את שתי ההתייחסויות שאיננו צריכים (`Approvals`, `SharePoint`). אחרי הפריסה הראשונה הדלת נסגרת.

### מצב התבנית

התבנית הרשמית קיימת ב-SharePoint של המכללה אך **טרם הורדה** (פעולת המשך 4).
**בינתיים נבנה מול כותרת ותחתית זמניות** — המבנה הלוגי של המסמך אינו תלוי בעיצוב הסופי.

### השלכות

| השלכה | פירוט |
|---|---|
| אין יצירת מסמכים בערכה | אין `Word Online`, אין `PopulateWordTemplate`, אין `ConvertFile`, אין המרת PDF — בשום מקום |
| **אין תקדים להורדת קובץ** | זהה ל-`D-02`: החזרת bytes לדפדפן היא עבודה חדשה לגמרי |
| **CSP חוסם מציג PDF מוטמע** | `object-src 'none'`, **אין `frame-src` כלל**, ו-`Inject-unsafe-eval: false`. ספריות PDF בצד לקוח נזקקות ל-`eval`. כל מציג מוטמע ידרוש עריכת CSP מכוונת דרך `/ppcs-add-csp-domain` |
| עברית ב-Word | תבניות Word עם RTL ומספרים לועזיים באמצע משפט הן עדינות ודורשות בדיקה |
| דרישת שחור-לבן | `§12.2` מחייב שהמסמך ישרוד הדפסה מונוכרומטית ללא אובדן משמעות — אילוץ על התבנית |
| אישור מוחלף | בפתיחה מחדש נוצר אישור חדש, והישן מסומן "הוחלף" **על פניו**; אף אחד מהם אינו נמחק |

---

## `D-04` — מנוע ההתאמות: plugin ב-C# שקורא כללים מטבלת Dataverse

**תאריך:** 2026-08-26

### מה השתנה — עדכון מהותי

הניסוח המקורי היה "plugin ב-`C#` עם מנוע כללים". **הניסוח המעודכן:**
**plugin ב-`C#` שקורא כללים כ*נתונים* מטבלת Dataverse, בתוספת שלב מחקר שנועד לגזור את הכללים האלה.**

ההבדל אינו סמנטי. בגרסה המקורית הכללים היו מקודדים בתוך הקוד; בגרסה הזו הם **נתונים** שמנהל/ת
יכול/ה לערוך במסך `S-A-02` בלי מפתח/ת ובלי פריסה.

> **🔴 זהו הרכיב הטכני בעל הסיכון הגבוה ביותר בפרויקט.** הוא חורג ממודל ה-starter כולו, נכתב
> בשפה שאינה שפת העבודה של המשתמש, ונשען על שלב מחקר שתוצאותיו טרם ידועות. ראו "סיכונים" בהמשך.

### זרימת הפיתוח — ארבעה שלבים

| שלב | שם | מה קורה | תוצר |
|---|---|---|---|
| **א** | מחקר | הרצת מודל עצי ההחלטה ב-Python (Google Colab) על 10–50 החלטות היסטוריות | עצי החלטה |
| **ב** | חילוץ | ניתוח העצים וזיהוי דפוסים חוזרים | רשימת כללים מועמדים בשפה אנושית |
| **ג** | קידוד | כתיבת הכללים כ**נתונים** בטבלת Dataverse | טבלת כללים מאוכלסת |
| **ד** | Plugin | plugin ב-C# שקורא את טבלת הכללים ומיישם אותם על בקשות חדשות | הרכיב הרץ בפועל |

### ⚠️ מודל ה-Python הוא כלי מחקר, לא רכיב ריצה

**המודל אינו מומר ל-C# ואינו רץ בייצור.** הוא משמש פעם אחת, בשלב א', כדי לעזור לנו להבין
אילו כללים בכלל קיימים בהחלטות ההיסטוריות. ה-plugin עצמו פשוט בהרבה ממודל למידת מכונה:
הוא קורא שורות מטבלה ומיישם תנאים.

זו ההבחנה החשובה ביותר בהחלטה הזו. מי שיקרא את הקוד בעוד שנה ויראה מחברת Colab במאגר, עלול
להסיק שיש כאן מודל ML בייצור. **אין.**

### נימוק

| שיקול | למה זה נבחר |
|---|---|
| **אין ML בזמן ריצה** | מהיר, דטרמיניסטי, ניתן לבדיקה. אותה בקשה תמיד מניבה אותן הצעות |
| **כללים ניתנים לעדכון על ידי מנהל/ת** | דרך `S-A-02`, בלי מפתח/ת ובלי פריסה — וזה בדיוק מה ש-`P-5` (מיכל) צריכה |
| **שקיפות מלאה** | ניתן להסביר "מדוע הוצע הפריט הזה" בהפניה לכלל מסוים שנורה. `R-02` מחייב שהבודק/ת יראה/תראה מה הניע את ההתאמה |
| **אין תשתית אימון מחדש** | אין תלות ב-retraining, ב-endpoint חיצוני או ב-Azure ML |

### מגבלות שהוסכמו במפורש

- **זו אינה למידה אמיתית.** עדכון כללים הוא פעולה ידנית.
- **המנהל/ת נדרש/ת לחשוב במונחי אם-אז.** זו קפיצה מושגית ביחס לתיאור ב-`P-5`, שם היא חושבת
  במונחי "הדרישה הזו בדרך כלל מסופקת על ידי X".
- **`S-A-02` הוא משטח רחב יותר מכפי שתוכנן.** במפרט המקורי הוא היה מסך ניהול כללים בשפה
  פשוטה; כעת הוא ממשק העריכה של מנוע ההתאמות כולו — כולל התצוגה המקדימה שמחייבת הרצה יבשה.

### פרטי מימוש

| נושא | החלטה |
|---|---|
| מיקום הקוד | תיקייה חדשה במאגר הזה: `plugins/scoring/` |
| מי מפתח | Claude Code. המשתמש אינו מתכנת/ת C#; צפויה עבודה באיטרציות |
| חומר מקור | מודל Python של המשתמש (Google Colab) — הנדסת מאפיינים, `MultiOutputClassifier` עם עצי החלטה לכל פטור, חיזוי מבוסס סף |
| מקור ה-C# הישן | **אינו זמין.** `_backup/SMKBCourseExemption.zip` מכיל רק את ה-DLL המהודר. אין ייבוא ממנו |

### סיכונים — לרשום אותם עכשיו, לא לגלות אותם אחר כך

| סיכון | הערכה |
|---|---|
| **10–50 דגימות הן מעט מאוד** | עם 30–40 דרישות, עץ החלטה לכל פטור מקבל מספר בודד של דוגמאות. סכנת התאמת-יתר גבוהה. **שלב ב' חייב להיות שיפוט אנושי על העצים, לא העתקה שלהם** |
| **הערכה חורגת ממודל ה-starter** | הערכה כולה מודלת טבלאות, משתני סביבה, flows ו-SPA. אין בה תמיכה ב-plugin assembly, אין דוגמה, ואין guard |
| **פריסה שונה** | plugin נפרס דרך Plug-in Registration Tool (`pac tool prt`) או כרכיב פתרון — לא דרך אף `deploy.ps1` קיים |
| **סכימת טבלת הכללים היא תכנון חדש** | היא צריכה לתמוך בכל סוגי הכללים שיתגלו בשלב ב', לפני שיודעים מהם |
| **התצוגה המקדימה ב-`S-A-02`** | מחייבת מצב "הרצה יבשה" של מנוע הכללים — הכפלה של משטח המנוע |

---

## `D-05` — קטלוג התוכניות והדרישות נבנה ידנית

**תאריך:** 2026-08-26

### מה השתנה

הדרישות של כל תוכנית יוזנו ידנית דרך מסך הניהול `S-A-01`. לא נבנה מנגנון ייבוא.

### נימוק

**אין קובץ ייבוא.** לא קיים קטלוג במבנה שניתן לקריאה מכונה, ולכן מנגנון ייבוא יצטרך מקור שאין לו.

**היקף הפיילוט הופך את זה לזניח:** תוכנית אחת בלבד — **חינוך מיוחד** — עם כ-30–40 קורסי חובה.
זמן ההזנה הידנית מוערך בכשעה.

### השלכות

`S-A-01` מתוכנן להזנת תוכנית אחת, ללא צורך ביכולות הזנה מרובה או ייבוא בשלב זה.
אם בעתיד יתווספו תוכניות רבות, זו תהיה החלטה נפרדת — ואז כדאי לשקול ייבוא, כי איכות הנתונים
בקטלוג היא הבסיס שעליו מנוע ההתאמות (`D-04`) עובד.

---

## `D-06` — מגבלות קבצי ראיות

**תאריך:** 2026-08-26

### מה השתנה

| פרמטר | ערך |
|---|---|
| סוגי קבצים | `PDF`, `JPEG`, `PNG` בלבד |
| גודל מרבי לקובץ | 4MB |
| מספר קבצים בהעלאה אחת | **5 לכל היותר** |
| מספר קבצים בבקשה | **ללא הגבלה** |

### נימוק

שלושת הסוגים מכסים את המציאות: גיליון ציונים כ-PDF, ותצלום של מסמך נייר מהטלפון כ-JPEG או PNG.
`§10.9` מחייב שצילום במצלמה יתקבל באותה טבעיות כמו קובץ.

**ההפרדה בין "ללא הגבלה בבקשה" ל-"חמישה בהעלאה"** נובעת ממגבלה טכנית ולא ממדיניות: כל העלאה
עוברת כ-base64 בקריאת flow אחת, ולכן אצווה גדולה היא מטען יחיד וכבד. `R-08` מחייב שאפשר להוסיף
מסמכים בכל שלב — ולכן אין סיבה להגביל את הסך הכולל.

### השלכה

ולידציה בצד השרת לפי `§16`: רשימת סיומות, בדיקת `magic bytes` על תחילת ה-base64 (`JVBER` ל-PDF,
`iVBORw0KGgo` ל-PNG, `/9j/` ל-JPEG), תקרת גודל לפי אורך ה-base64, ושם קובץ שהשרת מייצר.
קוד שגיאה גנרי אחד: `INVALID_FILE`.

---

## `D-07` — כתובת השולח

**תאריך:** 2026-08-26

### מה השתנה

| סביבה | שולח | נמען |
|---|---|---|
| Dev | חשבון השירות (בעל התייחסות החיבור) | כתובת המשתמש — **לצורך בדיקה בלבד** |
| Stage / Prod | תיבת דואר משותפת — **השם טרם נבחר** | הסטודנט/ית |

**המשתמש הוא הנמען ב-Dev, לא השולח.**

### נימוק

`§7.0` של המפרט קובע: **"אין כתובת `no-reply` בשום מקום במוצר הזה. הודעה שאדם אינו יכול לענות
עליה היא הודעה שמייצרת שיחת טלפון."** כל עשר ההודעות חייבות להגיע מכתובת מוסדית שמקבלת תשובות.

**האילוץ הטכני:** התייחסות החיבור `new_sharedoffice365_c3167` ("SMKB - Outlook") שולחת **בשם
הבעלים שלה** — חשבון שירות `SMKB User1`. שליחה בשם כתובת אחרת מחייבת הרשאת `Send-As`.
לכן זהו אילוץ ארכיטקטוני ולא הגדרה שממלאים בסוף.

### תלות

תיבת דואר משותפת עם `Send-As` היא **פעולת המשך 1 ו-3**. `Q-15` במפרט המקורי סימן את זה
כ"שער לפני זמינות כללית"; בפועל צריך לפתור אותו **לפני הפריסה הראשונה**, כי זהות החיבור
היא החלטת ארכיטקטורה.

---

## `D-08` — נעילת בקשה לבודק/ת

**תאריך:** 2026-08-26

### מה השתנה

נעילה בלעדית (`mutex`) על בקשה שנלקחה לבדיקה, עם **פקיעה אחרי 4 שעות**.
**השחרור הוא עצל (`lazy`)** — הפקיעה נבדקת כשמישהו/י צופה/ה בתור, לא על ידי תהליך מתוזמן.
מנהל/ת יכול/ה לשחרר בכפייה, ופעולה זו **מתווספת ל-`S-A-06`** ואינה מסך חדש.

### נימוק

`J-REV-01` שלב 2 מחייב בלעדיות: "הוא לוקח תיק — הוא שלו; אף אחד אחר לא יכול להחליט בו".
`P-4` (יוסי) חושש/ת במפורש מ"להחליט באותו תיק פעמיים".

**מדוע שחרור עצל ולא מתוזמן:** אין בערכה טריגר מתוזמן (`Recurrence`) כלל — לא flow, לא snippet.
תהליך מתוזמן לשחרור נעילות היה מחייב לכתוב טריגר חדש מאפס עבור פעולה שאפשר לבצע בחינם ברגע
שמסתכלים על התור. השחרור העצל נותן את אותה תוצאה בלי הרכיב הנוסף.

### השלכות

| השלכה | פירוט |
|---|---|
| אין פרימיטיבים לתור בערכה | אין מנגנון הקצאה, אין `claim`, אין עזר ל-optimistic concurrency, ואין דפוס "הפריט הבא" — לא ב-starters ולא במערכת העיצוב |
| אין `compare-and-swap` | ה-connector אינו תומך בכך. שני בודקים שלוחצים בו-זמנית — מרוץ אפשרי |
| ההחלטה המודעת | **המרוץ מתקבל.** בנפח הזה, קריאה חוזרת ואזהרה בפתיחת התיק מספיקות |

---

## `D-09` — מצב בהיר וכהה בפורטל; כלי הצוות בהיר בלבד

**תאריך:** 2026-08-26

### מה השתנה

הפורטל תומך במצב בהיר וכהה, כברירת מחדל לפי העדפת המערכת. כלי הצוות — בהיר בלבד.

### נימוק

`§8.1` קובע ש"בהיר וכהה שווים, לא ערכת נושא וּוריאנט" — הפורטל משמש קהל מגוון, לעיתים בטלפון,
לעיתים בשעות לילה. `§11.0` קובע לכלי הצוות: **"אין ערכות נושא ואין החלפת שפה"** — הגדרה שמסייעת
לסטודנט/ית חרד/ה שמשתמש/ת פעם אחת היא חיכוך למי שפותח/ת את הכלי בפעם התשיעית היום.

### השלכה — חיובית ולא יקרה

`SmkbThemeToggle` נכלל במערכת העיצוב, וה-Power Apps starter כבר מרכיב את `SmkbAppHeader` כשמתג
ערכת הנושא **כבוי**, בעוד ה-Code Site מייבא את אסימוני המצב הכהה. **זו כמעט בדיוק ברירת המחדל
המשולחת.** הרכיב אינו מתועד ב-`SMKB-UI.md` (אחד מ-17 רכיבים חסרים שם), ולכן יש לקרוא את קובצי
ההצהרות בחבילה ולא את התיעוד.

---

## `D-10` — הגנה מפני בוטים: Turnstile עם סוד ב-Azure Key Vault

**תאריך:** 2026-08-26

### מה השתנה

`Cloudflare Turnstile` במסך הכניסה `S-P-01`. מפתח הסוד נשמר ב-**Azure Key Vault**
דרך משתנה סביבה מסוג `Secret`.

### נימוק

`S-P-01` מגן על פעולה **לא-מאומתת** ששולחת דואר לכתובת שרירותית. המפרט מגדיר את זה כ"בדיקה
שקטה" שרוב האנשים לא ירגישו, ושבכשלון אומרת משהו נייטרלי — **לא** "נראה שאת/ה רובוט".

### תלות ב-IT — פעולת המשך 1

**אין נתיב ל-`Secret` שאינו עובר דרך Key Vault.** הדרישות:

| דרישה | פירוט |
|---|---|
| רישום ספק משאבים | `Microsoft.PowerPlatform` על המנוי |
| הרשאות RBAC | "Key Vault Secrets User" — גם למי שמגדיר/ה את הערך **וגם** ל-service principal של Dataverse, מזהה `00000007-0000-0000-c000-000000000000` |
| טננט | Key Vault והסביבה באותו טננט |
| חומת אש | ה-vault מתיר גישה |

### מלכודות שנרשמות מראש

- **משתנה `Secret` לעולם אינו נקרא דרך `parameters()`.** הוא מיובא בהצלחה ואז **ה-flow מסרב
  להידלק**. הקריאה היא דרך הפעולה `RetrieveEnvironmentVariableSecretValue` עם `secureData` על הפלט.
- **כשל סגור (`fail closed`)** — סניף הדחייה חייב `runAfter: [Succeeded, Failed, Skipped]`, כך
  ששגיאת Key Vault, שגיאת HTTP או תשובה שאינה ניתנת לפענוח כולן דוחות.
- **שלוש הנחיות CSP**, לא אחת: `script-src` + `frame-src` + `connect-src`.
- **ה-SPA אינו יכול לקרוא site settings**, ולכן מפתח האתר ה*ציבורי* חייב להגיע דרך flow תצורה.

---

## `D-11` — חוק הגנת הפרטיות הישראלי בלבד

**תאריך:** 2026-08-26

### מה השתנה

המשטר החל הוא **חוק הגנת הפרטיות הישראלי כפי שתוקן**. אין אוכלוסיית תושבי האיחוד האירופי,
ולכן GDPR אינו חל.

### נימוק

זו התשובה הזמנית ש-`Q-02` הניח, והיא אושרה. אוכלוסיית היעד היא מועמדים למכללה ישראלית.

### השלכה

מגדיר את `D-13` (תקופות שמירה) ואת השאלה האם נדרש מסלול גישה עצמית לנתונים.
הנחת המפרט היא שסטודנט/ית יכול/ה לצפות, לייצא ולבקש תיקון של המידע שמוחזק עליו/ה.

**כפוף לבדיקה משפטית — פעולת המשך 5.**

---

## `D-12` — יעד נגישות

**תאריך:** 2026-08-26

### מה השתנה

**רצפה:** תקן ישראלי `IS 5568` ושקילותו ל-`WCAG 2.0 AA`.
**יעד:** `WCAG 2.2 AA`.
**חל על שני המשטחים** — הפורטל וכלי הצוות.

### נימוק

`§9.4` מנמק את החלת התקן על כלי הצוות: **נגישות לעובד/ת היא חובת מעסיק, לא אדיבות** —
בודק/ת שאינו/ה יכול/ה להשתמש בכלי אינו/ה יכול/ה לעשות את עבודתו/ה. המכללה היא מוסד ציבורי.

### השלכות

| השלכה | פירוט |
|---|---|
| **אין שער נגישות בערכה** | אף אחד משני ה-starters אינו משלח או מתעד תקן, בדיקה או כלל lint לנגישות |
| `SmkbAccessibility` אינו תאימות | סרגל הנגישות שמגיע עם מערכת העיצוב הוא **ווידג'ט**, לא הוכחת תאימות |
| מנגנון הסקירה | `/ux-audit` בשורש + `audit/TEMPLATE-ux-audit.md` |
| כלי צוות צפוף ב-RTL | `WCAG 2.2 AA` על טבלה צפופה בעברית הוא עבודה ממשית — יש לתקצב אותה |

**סקירת רכז/ת הנגישות לפני קידום ל-Stage — פעולת המשך 6.**

---

## `D-13` — תקופות שמירה

**תאריך:** 2026-08-26

### מה השתנה

| פריט | תקופה | נמדדת מ- |
|---|---|---|
| אישורים והחלטות | **8 שנים** | תאריך הנפקת האישור (מאושר) או תאריך ההחלטה (נדחה) |
| קובצי ראיות | **סגירה + 90 יום** | כל מצב סופי |

**"סגירה" כוללת בקשות שבוטלו על ידי הסטודנט/ית**, לא רק בקשות שאושרו או נדחו.
כל המצבים הסופיים מפעילים את מחיקת הראיות אחרי 90 יום.

### נימוק

`§12.3` מנסח את העיקרון: **"לראיות שהועלו יש חיים קצרים יותר מההחלטה שהן תמכו בה."**
גיליון ציונים הוא הפריט הרגיש ביותר שהמערכת מחזיקה והכי פחות שימושי אחרי שהוחלט והוסבר.
מה שהופך את ההחלטה להגנתית הוא **הנימוק הכתוב של הבודק/ת**, לא הקובץ.

### השלכות

- נדרשת עמודת `PurgeAfter` על הראיות, מחושבת בעת הסגירה.
- נדרש תהליך מחיקה מתוזמן — **ואין טריגר מתוזמן בערכה** (ראו `D-08`, `D-16`).
- `§12.3` מחייב גם: **טיוטה שאינה מסתיימת אינה נמחקת ללא התראה מראש**.

**כפוף לבדיקה משפטית — פעולת המשך 5.**

---

## `D-14` — סדר עדיפות במדדי ההצלחה

**תאריך:** 2026-08-26

### מה השתנה

כאשר המדדים מתנגשים, סדר העדיפות הוא:
**1. מאמץ הבודק/ת · 2. זמן ההמתנה של הסטודנט/ית · 3. עקביות ההחלטות**

### נימוק

זו התשובה הזמנית של `Q-05`, והיא נגזרת מהמוצר עצמו: עצם קיומה של יכולת התאמה, יחד עם הכלל
ששום דבר אינו מאושר ללא אדם, מעידים שמאמץ הבודק/ת הוא הצוואר.

### השלכה

זהו שובר השוויון בהחלטות תכנון עתידיות. לדוגמה, הוא תומך בעיצוב `S-R-01` שמדגיש התיישנות
על פני מיון כללי, ובכך ש-`S-R-02` נמדד בראש ובראשונה לפי כמה מהר בודק/ת מגיע/ה להחלטה מבוססת.

---

## `D-15` — עברית בלבד

**תאריך:** 2026-08-26

### מה השתנה

אין מתג שפה. המוצר כולו בעברית.

### נימוק

`Q-07` מנמק זאת היטב: המכשול אינו הממשק אלא **התוכן**. כל תוכן שנכתב על ידי אדם — שמות קורסים,
שמות תוכניות, ובעיקר נימוקי הבודק/ת — קיים רק בעברית. מתג שפה היה מתרגם את המסגרת סביב תוכן
שאינו מתורגם, וזה נקרא **שבור**, לא דו-לשוני.

`§12.1` מחדד: **מה שנכתב על ידי בודק/ת לעולם אינו מתורגם** — תרגום נימוק דחייה שם מילים בפיו
של אדם ששמו מופיע על מסמך שהסטודנט/ית עשוי/ה להעביר הלאה.

### השלכה

מסיר שכבת i18n שלמה. ה-Code Site starter כבר עברית-ראשית: רק מחרוזות שגיאה וערוץ מתורגמות,
וכל שאר הטקסט בתבניות הוא עברית — מה שמתאים בדיוק להחלטה הזו.

---

## `D-16` — התראות לבודקים: משיכה עם תקציר יומי

**תאריך:** 2026-08-26

### מה השתנה

אין דחיפה לכל בקשה. יש **תור שנצפה ביוזמת הבודק/ת** (`S-R-01`), בתוספת **תקציר יומי אחד**
(`M-07`) ומחוון התיישנות גלוי.

### נימוק

`Q-11` מנמק: דחיפה לכל בקשה יוצרת מרוץ בין בודקים ותרבות של הפרעות, עבור עבודה שאינה דחופה
ברמת השעה. `M-07` **אינו נשלח כשהתור ריק** — הודעה יומית שאומרת "אין מה לעשות" מאמנת אנשים
להתעלם מההודעה שכן חשובה.

### השלכה

`M-07` הוא תהליך מתוזמן, **ואין טריגר מתוזמן בערכה** — לא flow ולא snippet. יש לכתוב אותו מאפס.
זהו אותו פער שמופיע גם ב-`D-13` (מחיקת ראיות) וב-`M-08` (התנצלות על איחור).

**מחוון ההתיישנות ב-`S-R-01` נושא משקל כבד מהרגיל.** במוצר הזה **אין דדליינים ואין מועדי הגשה**:
בקשות מתקבלות בכל עת, ואין חלון שנסגר ומכריח החלטה (`§1.4`). בהיעדר לחץ לוח השנה הזה, ובהיעדר דחיפה, הוא
הדבר היחיד שמונע מבקשה להישכח.

---

## `D-17` — אין סף כניסה אוטומטי

**תאריך:** 2026-08-26

### מה השתנה

אין סינון אוטומטי לפי ותק הלימודים הקודמים, ציון מינימלי או הכרה במוסד.
**כל בקשה מגיעה לבודק/ת.**

### נימוק

זו התשובה הזמנית של `Q-14`. `R-03` מנמק: אם קיימים תנאי סף, מקומם **במסך הכניסה, לפני שהסטודנט/ית
משקיע/ה מאמץ** — ולעולם לא כגילוי בתוך נימוק דחייה. תנאי שמתגלה אחרי ההשקעה נקרא כמלכודת.

### השלכה — עקבי עם אילוץ טכני

הציונים נשמרים כ**טקסט** ולא כמספר, כי `S-P-05` מחייב במפורש שסולם ציונים חריג ממוסד זר
לא יידחה על ידי טופס. המשמעות: **אי אפשר להשוות ציונים אריתמטית**, ולכן סף "ציון מינימלי"
היה קשה למימוש ממילא. ההחלטה והאילוץ מחזקים זה את זה.

---

## `D-18` — היסטוריית החלטות בין-תוכניתית נטענת בעצלתיים

**תאריך:** 2026-08-26

### מה השתנה

הפאנל ב-`S-R-02` שמציג החלטות קודמות של אותו/ה סטודנט/ית בתוכניות אחרות **נטען לפי דרישה**,
ואינו חלק במטען הראשוני של המסך.

### נימוק

`Q-13` (שנשאר נעול — ראו `D-19`) קובע שפטורים מתוכנית אחת **לעולם אינם מועברים** לאחרת: הם הקשר
לבודק/ת בלבד, מסומנים במפורש כהחלטה של תוכנית אחרת, וכל בקשה נשפטת לגופה.

ההחלטה הזו אינה משנה את הכלל — היא משנה **מתי** הנתון נטען. `S-R-02` הוא המסך הרגיש ביותר
לביצועים במוצר, והשאילתה חוצה את כל התוכניות של הסטודנט/ית עבור הקשר שלעיתים קרובות אינו נדרש.

---

## `D-19` — שבע ההחלטות הנעולות נשארות נעולות

**תאריך:** 2026-08-26

### מה השתנה

ההחלטות שסומנו במפרט כ"כבר הוכרעו" נשארות כאלה ואינן נפתחות מחדש — **למעט `Q-01` שבוטלה
על ידי `D-01`**.

> **תיקון לניסוח המקורי.** הרשימה נקראה תחילה "שמונה החלטות נעולות", אך `Q-01` הייתה אחת מהן,
> ו-`D-01` מבטלת אותה. **נשארות שבע.** יומן החלטות שסותר את עצמו בערך הראשון גרוע מהיעדר יומן.

| מזהה | ההחלטה | סטטוס |
|---|---|---|
| `Q-01` | הערכה מקדימה לפני הרשמה | ❌ **בוטלה על ידי `D-01`** |
| `Q-06` | עשרה ימי עבודה, מוצהרים בעת ההגשה | 🔒 נעולה |
| `Q-08` | ביטול בקשה אפשרי עד להקצאה לבודק/ת | 🔒 נעולה |
| `Q-09` | מסמכים נוספים בכל שלב; מסמך שהועלה אינו מוסר או מוחלף | 🔒 נעולה |
| `Q-10` | הסטודנט/ית אינו/ה רואה ציון, מספר או אפיון של ודאות | 🔒 נעולה |
| `Q-12` | אין פעולות בכמות; כל בקשה מקבלת החלטה פרטנית | 🔒 נעולה |
| `Q-13` | בקשה פעילה אחת לכל תוכנית; אין העברה בין תוכניות | 🔒 נעולה |
| `Q-15` | בעלות על תיבת הדואר | ✅ **הוכרעה על ידי `D-07`** |

### נימוק

`Q-08` ו-`Q-09` יחד הן הדוגמה הטובה ביותר לכך שהנעילה מוצדקת: כל אחת מהן לבדה סבירה, וביחד הן
פותרות את המלכודת שהמפרט מזהה — סטודנט/ית שהעלה/תה גיליון ציונים שגוי. ביטול לפני הקצאה הוא
שסתום השחרור, והוספת מסמך בכל שלב היא התיקון. פתיחה מחדש של אחת מהן שוברת את השנייה.

`Q-13` נשאר נעול, אך אופן המימוש שלו מעודכן בשני מקומות:

- **`D-18`** — ההקשר הבין-תוכניתי ב-`S-R-02` נטען בעצלתיים.
- **רמת האכיפה (הוכרע 2026-08-27)** — הכלל נאכף **ברמת הבקשה, לא ברמת המועמד/ת**: הייחודיות
  היא על הצירוף `CandidateId` + `ProgrammeId` + סטטוס פעיל בשורת הבקשה. אכיפה ברמת המועמד/ת
  הייתה מגבילה לבקשה פעילה אחת בסך הכול וסותרת את החצי השני של הכלל ("כמה תוכניות שתרצו").
  האכיפה נעשית ב-`CEX-PortalStartApplication` לפני היצירה. ראו `SOLUTION-SPEC.md` §3, בלוק `Application`.

### תסדיר קוד האימות על האישור (הוכרע 2026-08-27)

קוד האימות המודפס על אישור הפטור משתמש בתחילית **`Cert-`** ולא `VER-`: `Cert-XXXXXXXX`.
הערך אקראי ולא רץ, ומרחב התווים אינו כולל תווים מתבלבלים (`0`/`O`, `1`/`I`) כדי שהקלדה
מדף מודפס תעבוד. שייך ל-`D-03` (האישור כמסמך) ומפורט ב-`SOLUTION-SPEC.md` §3, בלוק `Certificate`.

---

## `D-23` — האישור הוא הערכה מייעצת, לא מסמך מחייב

**תאריך:** 2026-08-30

> **מספור:** `D-20`–`D-22` אינם בשימוש. הפער מכוון, כמו `H3`/`H6` ב-`INIT_PROJECT.md` —
> מספור מחדש היה מפנה בשקט כל הפניה קיימת.

### מה השתנה

האישור שהמערכת מפיקה מוגדר מחדש כ**הערכה מייעצת**. אין לו תוקף משפטי והוא אינו מחייב את המדור
לענייני סטודנטים. במפרט המקורי (`§12.2`) הוא תואר כ"נקודת הסיום היחידה" — המסמך שהסטודנט/ית
מחזיק/ה ומוכיח/ה באמצעותו.

### נימוק

ההגדרה נבחנה מחדש מול דרישות התבנית בפועל. המכללה אינה מוכנה שמערכת אוטומטית תייצר התחייבות
מוסדית; ההחלטה הסופית נשארת אצל הגורם האקדמי המוסמך בעת ההרשמה ללימודים. האישור **מתעד** שיפוט
מקצועי — הוא אינו מחליף אותו.

### השלכות

| תחום | שינוי |
|---|---|
| תוקף | אין תוקף משפטי; אינו מחייב את המדור |
| שפה | `הערכה` ו-`החלטה מייעצת` במקום `אישור סופי` |
| `M-03` | ההודעה אינה מבטיחה פטור בפועל |
| `S-P-08` / `S-P-10` | הורדת האישור מלווה בהסבר על אופייה המייעץ |
| תבנית האישור | פסקת הסתייגות **בולטת** — ממוסגרת, מוצללת, מודגשת, 14pt, ממורכזת. לא באותיות קטנות |

**נוסח ההסתייגות (מאושר):**
`תעודה זו הינה הערכה בלבד ועשויה להשתנות בעת הגשת בקשה ללימודים עצמם`

### תסדיר קוד האימות

`Cert-XXXXXXXX` — התחילית `Cert-` ואחריה **שמונה** תווים אלפאנומריים גדולים, אקראיים ולא רצים,
ממרחב שאינו כולל תווים מתבלבלים (`0`/`O`, `1`/`I`). שייך ל-`D-03` ול-`O-5`.

> **אי-התאמה קטנה שנרשמת ולא מוסתרת:** התיאור שהתקבל אמר "12 תווים סך הכול", אך `Cert-` הוא
> חמישה תווים ועוד שמונה — כלומר **13**. התסדיר עצמו הוא המחייב; הספירה תוקנה.

### היחס ל-`D-01`

**אינו מבטל את `D-01`.** `D-01` ביטל את ההבחנה בין *שני סוגי* פונים; כאן **כל** האישורים מייעצים
באותה מידה. אין הכפלת קטגוריות — יש הורדת מעמד אחידה.

### ⚠️ מתח מוכר מול `PRODUCT_SPEC.md` — נרשם, לא מוסתר

`§12.2` ו-`§1.4` מתארים את האישור כנושא את מלוא התוצאה, ו-`R-01` מבטיח "לא תלמד/י את הקורס".
**`§1.4` גם קובע שאין מערכת במורד הזרם שמקבלת את ההחלטה.** אם האישור אינו מחייב ואין נמען —
אף גורם אינו הופך את הפטור למציאות.

ההחלטה נרשמת כפי שהתקבלה. **יישוב המתח — מי כן הופך פטור לתקף — נותר פתוח כ-`O-7`.**

---

## מה הלאה

הצעד הבא הוא `SOLUTION-SPEC.md` — המפרט הטכני. הקובץ הזה הוא אחד ממקורותיו, יחד עם
`PRODUCT_SPEC.md` ושלושת סקרי ה-starters.

**החלטות שיש לקבל לפני הפריסה הראשונה (שלב `8.1a`):**

| החלטה | מדוע דחוף |
|---|---|
| התייחסות החיבור ל-Word Online (`D-03`) | אינה ניתנת להסרה על ידי ייבוא חוזר |
| גזימת התייחסויות חיבור שאינן בשימוש | `Approvals` ו-`SharePoint` — אותה חד-כיווניות |
| סוגי משתני סביבה | סוג `JSON` אינו ניתן לשינוי על ידי ייבוא חוזר |
| משתני ה-ALM המשולחים | `OtpDailyCap`, `SecurityAlertEmails` — הסרה דורשת שלוש עריכות מתואמות |
